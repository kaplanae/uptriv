<svg viewBox="0 0 200 220" fill="none" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <filter id="shadow" x="-20%" y="-20%" width="140%" height="140%">
      <feDropShadow dx="0" dy="4" stdDeviation="4" flood-color="#000" flood-opacity="0.3"/>
    </filter>
    <clipPath id="wheelClip">
      <circle cx="100" cy="115" r="75"/>
    </clipPath>
  </defs>

  <!-- Stopwatch body -->
  <g filter="url(#shadow)">
    <!-- Outer ring -->
    <circle cx="100" cy="115" r="85" fill="#1a2744" stroke="#2d4a7c" stroke-width="4"/>

    <!-- Inner colored wheel -->
    <g clip-path="url(#wheelClip)">
      <!-- News - Coral Red (top right) -->
      <path d="M100 115 L100 40 A75 75 0 0 1 165 77.5 Z" fill="#FF6B6B"/>
      <!-- History - Teal (right) -->
      <path d="M100 115 L165 77.5 A75 75 0 0 1 165 152.5 Z" fill="#4ECDC4"/>
      <!-- Science - Blue (bottom right) -->
      <path d="M100 115 L165 152.5 A75 75 0 0 1 100 190 Z" fill="#45B7D1"/>
      <!-- Entertainment - Green (bottom left) -->
      <path d="M100 115 L100 190 A75 75 0 0 1 35 152.5 Z" fill="#96CEB4"/>
      <!-- Sports - Yellow (left) -->
      <path d="M100 115 L35 152.5 A75 75 0 0 1 35 77.5 Z" fill="#FFEAA7"/>
      <!-- Geography - Plum (top left) -->
      <path d="M100 115 L35 77.5 A75 75 0 0 1 100 40 Z" fill="#DDA0DD"/>
    </g>

    <!-- Category icons -->
    <!-- News - Globe icon (top right) -->
    <g transform="translate(130, 58)">
      <circle cx="0" cy="0" r="14" fill="rgba(0,0,0,0.2)"/>
      <text x="0" y="6" text-anchor="middle" font-size="18">🌍</text>
    </g>

    <!-- History - Scroll icon (right) -->
    <g transform="translate(152, 115)">
      <circle cx="0" cy="0" r="14" fill="rgba(0,0,0,0.2)"/>
      <text x="0" y="6" text-anchor="middle" font-size="18">📜</text>
    </g>

    <!-- Science - Microscope icon (bottom right) -->
    <g transform="translate(130, 172)">
      <circle cx="0" cy="0" r="14" fill="rgba(0,0,0,0.2)"/>
      <text x="0" y="6" text-anchor="middle" font-size="18">🔬</text>
    </g>

    <!-- Entertainment - Movie icon (bottom left) -->
    <g transform="translate(70, 172)">
      <circle cx="0" cy="0" r="14" fill="rgba(0,0,0,0.2)"/>
      <text x="0" y="6" text-anchor="middle" font-size="18">🎬</text>
    </g>

    <!-- Sports - Trophy icon (left) -->
    <g transform="translate(48, 115)">
      <circle cx="0" cy="0" r="14" fill="rgba(0,0,0,0.2)"/>
      <text x="0" y="6" text-anchor="middle" font-size="18">🏆</text>
    </g>

    <!-- Geography - Map icon (top left) -->
    <g transform="translate(70, 58)">
      <circle cx="0" cy="0" r="14" fill="rgba(0,0,0,0.2)"/>
      <text x="0" y="6" text-anchor="middle" font-size="18">🗺️</text>
    </g>

    <!-- Center circle with text -->
    <circle cx="100" cy="115" r="45" fill="#0f1729"/>
    <circle cx="100" cy="115" r="42" fill="#1a2744" stroke="#2d4a7c" stroke-width="2"/>

    <!-- UpTriv text -->
    <text x="100" y="110" text-anchor="middle" font-family="'Space Grotesk', sans-serif" font-size="22" font-weight="700" fill="#667eea">Up</text>
    <text x="100" y="132" text-anchor="middle" font-family="'Space Grotesk', sans-serif" font-size="22" font-weight="700" fill="#764ba2">Triv</text>

    <!-- Stopwatch top button -->
    <rect x="90" y="18" width="20" height="15" rx="3" fill="#2d4a7c"/>
    <rect x="93" y="10" width="14" height="12" rx="2" fill="#1a2744" stroke="#2d4a7c" stroke-width="2"/>

    <!-- Stopwatch side button -->
    <rect x="170" y="70" width="12" height="20" rx="2" fill="#2d4a7c" transform="rotate(30, 176, 80)"/>
  </g>

  <!-- Tagline -->
  <text x="100" y="212" text-anchor="middle" font-family="'Inter', sans-serif" font-size="12" font-weight="500" fill="#a0a0b0">6 Questions • 60 Seconds</text>
</svg>
